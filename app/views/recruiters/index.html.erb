<turbo-frame id="content_panel">
<%= render 'shared/breadcrumbs', breadcrumbs: @breadcrumbs %>

<div class="max-w-6xl">
  <h1 class="text-2xl font-bold mb-1 text-orange-500">Recruiter</h1>
  <div class="text-sm text-gray-400 mb-6">Available NPCs for hire (Level <%= current_user.level_tier %> pool)</div>

  <%# Pool Refresh Timer %>
  <% if @next_refresh %>
    <div class="bg-blue-900 border border-blue-700 p-4 mb-6">
      <div class="flex items-center justify-between">
        <span class="text-gray-400">Pool refreshes in:</span>
        <span class="text-lime-400 font-mono"
              data-controller="countdown"
              data-countdown-arrival-value="<%= @next_refresh.iso8601 %>"
              data-countdown-frame-value="content_panel">
          <%= distance_of_time_in_words_to_now(@next_refresh) %>
        </span>
      </div>
    </div>
  <% end %>

  <% if @recruits.any? %>
    <div class="space-y-3">
      <% @recruits.each do |recruit| %>
        <div class="bg-blue-900 border border-blue-700 p-4 hover:border-orange-500 transition-colors">
          <%# Header: Class, Rarity, Race %>
          <div class="flex items-center justify-between mb-3">
            <div>
              <span class="font-bold text-white"><%= recruit.npc_class.humanize %></span>
              <span class="text-xs bg-<%= rarity_color(recruit.rarity_tier) %>-800 text-white px-2 py-0.5 ml-2 rounded">
                <%= recruit.rarity_tier.upcase %>
              </span>
              <span class="text-gray-400 ml-2">(<%= recruit.race.humanize %>)</span>
            </div>
            <div class="text-sm">
              <span class="text-gray-400">Skill: </span>
              <span class="text-<%= skill_color(recruit.skill) %>-400 font-bold">
                <%= recruit.skill %>
              </span>
            </div>
          </div>

          <%# Employment History Preview %>
          <div class="mb-3 text-sm">
            <span class="text-gray-500">Employment history:</span>
            <% if recruit.employment_history.is_a?(Array) && recruit.employment_history.any? %>
              <% last_job = recruit.employment_history.last %>
              <span class="text-gray-400 ml-2">
                <%= last_job["employer"] || last_job[:employer] %> —
                <span class="text-<%= outcome_color(last_job["outcome"] || last_job[:outcome]) %>-400">
                  <%= last_job["outcome"] || last_job[:outcome] %>
                </span>
              </span>
            <% else %>
              <span class="text-gray-500 ml-2">No prior employment</span>
            <% end %>
          </div>

          <%# Hiring Info %>
          <div class="flex items-center justify-between mt-4 pt-3 border-t border-blue-700">
            <div class="text-sm">
              <span class="text-gray-400">Hire cost:</span>
              <span class="text-orange-400 font-bold ml-1"><%= recruit.base_wage * 2 %> cr</span>
              <span class="text-gray-500 ml-2">(2 weeks advance)</span>
            </div>
            <div class="flex gap-2">
              <%= link_to recruiter_path(recruit), class: "px-3 py-1 bg-blue-800 text-gray-300 hover:text-white rounded text-sm", data: { turbo_frame: "content_panel" } do %>
                <span class="text-lime-400">v</span> View Resume
              <% end %>
              <%= button_to hire_recruiter_path(recruit),
                  method: :post,
                  class: "px-4 py-1 bg-lime-800 text-white hover:bg-lime-700 rounded text-sm font-bold" do %>
                <span class="text-white/70">h</span> Hire
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <%# Enhanced empty state for new players %>
    <div class="bg-blue-900 border border-blue-700 p-8">
      <div class="text-center mb-6">
        <div class="text-4xl mb-3">👷</div>
        <h2 class="text-xl font-bold text-orange-400 mb-2">No Recruits Available</h2>
        <p class="text-gray-400">The recruit pool is currently empty at your level.</p>
        <p class="text-gray-500 text-sm italic mt-2"><%= FlavorText.for(:npc_hired) %></p>
      </div>

      <div class="bg-blue-950 border border-blue-800 p-4 rounded mb-4">
        <h3 class="text-lime-400 font-bold mb-2">💡 About Workers</h3>
        <p class="text-gray-300 text-sm">
          Workers help your fleet operate more efficiently. Hire Navigators to plot faster routes,
          Engineers to maintain your ships, Marines for protection, and Governors to manage stations.
        </p>
      </div>

      <div class="text-center">
        <p class="text-gray-400 text-sm">
          🕐 <span class="text-lime-400">Check back soon</span> — new recruits appear regularly as the pool refreshes.
        </p>
      </div>
    </div>
  <% end %>

  <%= link_to workers_path, class: "block mt-6 text-sky-400 hover:text-sky-300", data: { turbo_frame: "content_panel" } do %>
    ← Back to Workers
  <% end %>

  <div class="mt-8 text-sm text-gray-500">
    <p>Keyboard: j/k to navigate, Enter to view resume, h to hire, c to compare</p>
  </div>
</div>
</turbo-frame>
