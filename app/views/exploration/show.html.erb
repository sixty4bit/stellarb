<turbo-frame id="content_panel">
<%= render 'shared/breadcrumbs' %>

<div class="max-w-6xl">
  <h2 class="text-2xl font-bold mb-4 text-orange-500">🔭 Exploration</h2>

  <div class="bg-blue-900 border border-blue-700 rounded p-6 space-y-4">
    <p class="text-gray-300">Explore the galaxy systematically using different exploration modes.</p>

    <div class="flex flex-wrap gap-4">
      <%= button_to "🌀 Growing Arcs", growing_arcs_exploration_path,
          method: :post,
          class: "bg-purple-600 hover:bg-purple-500 text-white font-medium px-4 py-2 rounded transition-colors",
          data: { turbo_frame: "_top" } %>
    </div>

    <% if current_user.explored_coordinates.any? %>
      <div class="mt-6">
        <h3 class="text-lg font-semibold text-orange-400 mb-2">Recently Explored</h3>
        <ul class="text-sm text-gray-300 space-y-1">
          <% current_user.explored_coordinates.order(created_at: :desc).limit(10).each do |coord| %>
            <li>
              (<%= coord.x %>, <%= coord.y %>, <%= coord.z %>)
              <%= "⭐" if coord.has_system %>
            </li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>
</div>
</turbo-frame>
